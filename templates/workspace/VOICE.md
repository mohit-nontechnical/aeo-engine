# {{CLIENT_NAME}} — Voice

Generated from intake. Two sections: judgment rules shape writing; deterministic rules are
enforced literally by the lint skill (one exact string or pattern per line).

## Judgment rules

- Tone: {{e.g. "operator explaining to a peer, not a marketer"}}
- Stance: {{e.g. "contrarian on X, conservative on Y — see BRAND.md POVs"}}
- Structure: {{e.g. "short paragraphs (1-4 sentences), scannable h2/h3"}}
- Reading level: {{...}}
- How they explain: {{cite specifics from EXAMPLES/ — what makes their good example good}}
- What they hate: {{from the disliked example — why it fails}}

## Deterministic rules (lint greps these)

### Banned phrases
{{one per line, exact strings}}

### Punctuation / formatting
{{e.g. "no em-dashes or en-dashes anywhere — use commas, colons, parentheses, periods"}}

### Terminology (always / never)
{{e.g. 'say "customers", never "end-users"'}}

### Competitor naming
{{policy: may name / may not name / only in comparisons}}

## Approved samples

See `EXAMPLES/`. Reread one before each writing session — calibrate from the source, not memory.
